---
title: Basic data types in Python
teaching: 30
exercises: 30
questions:
  - What are the most commonly used data types in Python?
  - How do the behaviours of the data types differ from each other?
  - How do we modify data?
objectives:
  - Introduce learners to core data types.
  - Explain the difference between mutable and immutable data types.
  - Introduce slicing and indexing in Python.
keypoints:
  - Different data types have different behaviors.
  - We can perform type coercion to convert between types.
  - Indexing in Python starts from `0`, and is open-ended.
---

# Learning outcomes

{:.callout}

> In the previous section, we got our first taste of working with data in Python. In it, we queried the weather API to obtain the 24 hour forecast for Singapore's weather on 1st Jan 2025. We saw how the data was returned as a `json` object. We also learned about working with `dictionaries`, a fundamental data class in Python.

In this section, we will dive a bit more into some of the other common data types that are used in Python. By the end of this section, you should be able to:

1. Select the most appropriate data type for your use case.
2. Perform simple operations using these data types.
3.
